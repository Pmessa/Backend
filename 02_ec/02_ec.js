let boolean = false;
//boolean = boolean ?? "reasigna el valor si es null o undefined"
console.log(boolean);

let cero = 0;
//cero = cero || "reasigna el valor "
console.log(cero);

let nula = null;
//nula = nula ?? "sí, es nula"
console.log(nula);

const datos = {
  nombre: "Pablo",
};

datos.apellido ?? "Messa";
console.log(datos);
